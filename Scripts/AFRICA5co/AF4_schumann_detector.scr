// AF4_schumann_detector
// -------------------------------------------------------------------------------------------------------------

  FRAME schumann;			FRM_FindFrame(schumann, "AF4_23");
  FRAME sniper;			FRM_FindFrame(sniper, "AF4_31");

// -------------------------------------------------------------------------------------------------------------

Whenever inrange ((_FrameInRange(schumann) < 20) AND (!_LoadGameValue(88))){
  if(_ACTOR_GetState(sniper)){
    SendSignal(sniper, 9);
    Delay(2000);
    
  }
}

// -------------------------------------------------------------------------------------------------------------