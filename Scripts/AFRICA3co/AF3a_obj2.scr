// AF3a_obj2 - detecting location
// -------------------------------------------------------------------------------------------------------------

// -------------------------------------------------------------------------------------------------------------
Whenever detector(_PlayerInRange(8)){
  SUBTITLES_SetOn(true);
  SUBTITLES_SetText(07991404);
  Delay(2000);
  SUBTITLES_SetOn(false);
  SetObjectiveStatus(1, 1);
}

// -------------------------------------------------------------------------------------------------------------
